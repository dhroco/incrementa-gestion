/** Ruta absoluta del listado de contadores (Administrador de plataforma). */
export const ACCOUNTANTS_LIST_PATH = '/app/admin-global/contadores'

/** Ruta absoluta del listado de usuarios plataforma (Administrador de plataforma). */
export const PLATFORM_USERS_LIST_PATH = '/app/admin-global/usuarios-plataforma'

/** Ruta absoluta del listado de templates estándar (Administrador de plataforma). */
export const STANDARD_TEMPLATES_LIST_PATH = '/app/gestion-contratos/templates-estandar'

/** Ruta absoluta del listado de templates por empresa (Contador / Usuario empresa administrador). */
export const COMPANY_TEMPLATES_LIST_PATH = '/app/gestion-contratos/templates-por-empresa'
