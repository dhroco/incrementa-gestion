/**
 * Este seed era específico de Supabase Auth (auth.users) y ya no aplica.
 * La gestión de usuarios ahora es responsabilidad de Keycloak.
 * Conservado como no-op para no alterar la numeración de seeds.
 */
exports.seed = async function seed(_knex) {
  // no-op
}
